version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: PLACEHOLDER_TASK_DEFINITION
        LoadBalancerInfo:
          ContainerName: legal-container
          ContainerPort: 80

Hooks:
  ValidateService:
    - location: ./scripts/validate_service.sh
      timeout: 300
