version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: PLACEHOLDER_TASK_DEFINITION
        LoadBalancerInfo:
          ContainerName: legal-container-prod
          ContainerPort: 80
